<h1>Editing Books</h1>

<%= render "form", book: @book, url: create_books_path %>

<br>

<div>
  <%= link_to 'about book', show_boks_path(user_id: @current_user._id, books_id: @book.id)%>
  <%= link_to "Back to books", user_books_path(user_id: @current_user.id) %>
</div>
